{
  "name": "pomodoro_app",
  "version": "1.0.0",
  "description": "App that helps you with the Pomodoro technique",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "electron-packager . PomodoroApp --platform=win32 --arch=x64 --out=dist --overwrite --icon=icon.ico --asar"

  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@electron/packager": "^18.3.6",
    "electron": "^36.2.0"
  }
}
